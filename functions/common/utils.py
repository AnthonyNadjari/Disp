# scaffolding repo de test — NE PAS recopier dans le vrai repo.
# Stub minimal : seul get_n_exp_from_date est importé par functions.dispersion.__init__.
def get_n_exp_from_date(maturity_date, tickers):
    raise RuntimeError(
        "test-repo scaffolding: functions.common.utils.get_n_exp_from_date "
        "needs the real repo (exchange calendars).")
